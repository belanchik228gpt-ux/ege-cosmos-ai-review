export function spatialPlaneNames(labels: unknown): string[];
export function validSpatialPlaneValues(values: unknown): boolean;
export function renderSpatialPlane(
  values: unknown,
  labels?: unknown,
  progress?: number,
): string | null;
