# Native live teaching review — installed EXE and installed runtime

Проверено 8 сентября 2026 года, **17:32:58–17:34:06 UTC**. **9/9 ограниченных функциональных сценариев PASS**, 4 настоящих GPU-ответа локальной модели, 0 material fallback. Этот отдельный повтор подтверждает работу runtime и весов именно из установленного комплекта после NSIS-установки. Установку и сверку комплектности выполнил основной агент; здесь проверен реальный запуск установленного приложения через интерфейс.

- EXE: `C:\new cosmos\test-results\installed-app\EGE Cosmos.exe`, приложение сообщает `0.4.0`, `packaged: true`.
- Проверенный SHA-256 `resources/app.asar`: `375ff6d3937fbd7e4a0232692433c94d45cf3571cac4e8467aa0492489b982c3`.
- `COSMOS_RUNTIME_DIR=C:\new cosmos\test-results\installed-app\resources\runtime`.
- Прочитанный у работающего процесса `ExecutablePath`: `…\installed-app\resources\runtime\llama\llama-server.exe`.
- Аргумент модели `-m`: `…\installed-app\resources\runtime\models\Qwen3-8B-Q4_K_M.gguf`.
- Доказательство пути процесса: [runtime-process.json](runtime-process.json); временный ключ локального API удалён. Runtime не копировался и не заменялся во время прогона.
- Свежий отдельный профиль `…\test-results\teaching-live-native-1788888777997`; действия только UI, чтение состояния и диагностики без записи тестового состояния. Пользовательский профиль не затронут.

Использован тот же `scripts/teaching-live-final-review.mjs` с отдельной выходной папкой. Единственное изменение QA-скрипта — поддержка `COSMOS_RUNTIME_DIR` из окружения; продукт не менялся. Более ранний результат с workspace runtime сохранён в [отдельном отчёте](../teaching-live-native-0.4-verified/REVIEW.md).

## Девять проверок

| Сценарий | Время UI | Наблюдаемый результат |
|---|---:|---|
| Объяснение `\|x\|=0` | 22,925 с | GPU-модель, `zero-coordinate`, сохранены урок и taskIndex 4 |
| Неуверенный ответ `0? я не знаю` | 0,915 с | Координата принята отдельным маленьким шагом, сомнение сохранено |
| `что дальше` | 0,914 с | Основной вопрос о числе разных корней, 0 вызовов модели/планировщика |
| Просьба объяснить действительные корни | 4,697 с | GPU-модель, определение и подготовительный вопрос `x=2`, `x+1=3` |
| Непонимание слов «чему равно» | 4,557 с | GPU-модель, `radicals-1-square-value-meaning`, простой вопрос `3 − 5` |
| Ответ `-2` | 0,589 с | Принят подготовительный шаг |
| Возврат `дальше` | 0,928 с | Восстановлен `radicals-1-square`, исходный вопрос `(−5) · (−5)` |
| Реальная отмена запросом-ответом `25` | 7,916 с, включая 7 с наблюдения | До отправки `busy: true`, GPU, видимое ожидание; зарегистрирован AbortError; принятый шаг и последнее сообщение после ожидания не изменились |
| Обсуждение модуля после досрочного итога | 7,051 с | GPU-модель ответила, `summary` сохранён; ID нового ответа отличается от ID итога, все 15 ID уникальны |

Счётчики: **8 завершённых записанных ходов + 2 ввода отмены; 4 model, 0 material, 4 candidate, 4 review, 4 supported, 0 planner decisions**. Нет pageerror. Детерминированная обратная связь/навигация и отменённый запрос не засчитаны в число модельных ответов. Начальный запуск GPU — 19,004 с; после отмены повторный запуск — 2,888 с. Время UI включает служебные ожидания проверки; это измерение одного прогона, не обещание постоянной скорости.

## Что лично видно и прочитано

Лично просмотрены **все 9 `01…09-viewport.png`** размером 1920×1080, плюс отдельные кропы диалога №05 и №09. Все восемь текстовых ответов прочитаны из фактического результата. Сцена, условие, компас и поле отправки доступны одновременно в этих кадрах; нет горизонтального обрезания текста или формул. Технические сообщения модели не показаны в учебной реплике.

На №01 совпадают две точки в нуле; преподаватель спрашивает координату, затем отдельно возвращается к количеству корней. На №04 отображается подстановка `x + 2 = 5 → 3 + 2 = 5`, помеченная отдельным примером; активный вопрос использует другие числа. На №05 числовая прямая иллюстрирует движение влево на примере `2 − 4`; вопрос ученику `3 − 5` остаётся отдельным. На №07/08 доска показывает квадрат четвёрки, а ученик отвечает на квадрат пятёрки. На №09 итог не блокирует новый разговор; новый модельный ответ о модуле виден и читаем.

Реальный ответ №05: «“Чему равно” — это вопрос о результате. Например, если спрашивают “чему равно 3 − 5”, они хотят знать, какое число получится после вычитания. Попробуй посчитать 3 − 5». Далее задан самостоятельный вопрос; ответ −2 заранее не выдан. Здесь модельная реплика менее подробна, чем в предыдущем прогоне, но детур и визуальная поддержка присутствуют.

Реальный ответ №09: «Модуль числа — это его абсолютная величина. Он показывает, насколько число отличается от нуля, без учёта знака. Например, модуль −5 равен 5, а модуль 5 тоже равен 5». Смысл и примеры верны для проверенного запроса; это краткая справка, не полный урок.

## Границы и оставшиеся замечания

- Малое замечание №06 сохраняется: фраза «Вернёмся к выражению с буквой» не совпадает с непосредственным возвратом к числовому квадрату. Состояние возвращается правильно; нового изменения продукта не делалось.
- Один и тот же вопрос повторяется в нескольких областях. Нет оценки «идеально» или универсального качества нейросети по факту ответа.
- Диалог имеет внутреннюю прокрутку; перед снимком скрипт показывает последнее сообщение. Вся история не обязана помещаться в одном кадре.
- Проверен установленный путь через явный runtime override; автоматический поиск runtime без переменной окружения этим прогоном отдельно не проверялся.
- Это девять сценариев двух математических тем при 1920×1080, а не проверка всех предметов, всех сцен, OCR, системного DPI, CPU-fallback или качества на произвольных запросах. Другие проверки имеют отдельные отчёты.
- Второй проход проверки ответа использует локальные опоры; отметка `supported` не означает независимую или текущую онлайн-проверку каждого утверждения.
- Холодный запуск около 23 секунд до первого ответа остаётся ощутимым ожиданием. История более раннего таймаута под нагрузкой сохранена в `teaching-live-native-0.4-final`; этот прогон её не стирает.

Окно закрыто в 17:34:06 UTC, штатное завершение скрипта exit0. После закрытия отдельно подтверждено **0 процессов `llama-server.exe`**. Native/GPU очередь передана основному агенту; дальнейших запусков в этой задаче нет.
