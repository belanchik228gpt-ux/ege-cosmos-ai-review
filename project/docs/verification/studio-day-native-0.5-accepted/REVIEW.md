# Studio 0.5 — итоговая проверка дневного плана без GPU

Дата: 8 сентября 2026 года. Настоящий упакованный `release/win-unpacked/EGE Cosmos.exe`, ASAR SHA-256 `d75fb7d4e00fae1ec67e8c4bb5261ed6da91f5f3b481b890ecfbcf744ca67cfe`. Изолированный профиль указан в `report.json`; пользовательский профиль не использовался.

**Результат: 12/12 проверок PASS, 0 pageErrors.** EXE запущен с `--disable-gpu`. Полученный от Electron статус подтверждает программную композицию и растеризацию, недоступность WebGL/WebGPU. Низкое качество и reduced motion выбраны через интерфейс до начала дневного сценария и сохранились после перезапуска.

Пройден реальный маршрут: план 120 минут → начало дня → текущая тема и семь блоков → ввод черновика → пауза → продолжение → завершение первого учебного блока → перерыв → пропуск русского языка → досрочное завершение → экспорт HTML → возвращение в комнату и историю → поиск → перезапуск. Модельные запросы и авторизация не выполнялись; ответы и учебные факты не внедрялись через тестовый API.

Проверенные результаты:

- В активном дне видны тема «Модуль числа и расстояние», 25 плановых минут блока, 120 минут дня и счётчик 0 из 7.
- Поле ввода дневного урока при 1280×720 полностью находится в viewport: верх 623.70 px, низ 685.70 px, высота 62 px, шрифт 14 px. Лично проверен кадр `02b-day-composer-1280.png` с введённым текстом.
- После реальных пяти секунд активности пауза сохраняет `activeMs=5000` при ожидании ещё 5.3 секунды; возобновление сохраняет тот же черновик. Это проверка черновика при паузе, не обещание сохранения неотправленного текста после перезапуска.
- Завершение и пропуск имеют разные статусы. Итог содержит один завершённый учебный блок, один завершённый перерыв, один пропущенный учебный блок и два незавершённых учебных блока. Плановые завершённые 30 минут не подменяют фактически измеренное время.
- Самостоятельных попыток и правильных ответов 0. Кнопка завершения блока не повышает освоение. После досрочного завершения беседа сохранена, а её маршрут не получает ложные галочки всех пройденных этапов.
- Реальный HTML появился на диске, имеет 6775 байт и SHA-256 `eaeae1de5604852cca5bca09cac97c7d862d44082fec22c4f3b084b20250551b`. Файл открыт в отдельном настоящем окне Electron и снят в `07-real-exported-html.png`; это не подставленный предпросмотр. После перезапуска проверены запись документа и те же байты файла.
- Возврат из вторичного экрана сохраняет выбранную беседу. История открывает существующую запись без дублирования. Она честно показывает 0 сообщений, поскольку диалог модели в этой серии не запускался.
- В ориентире базового ЕГЭ действительно 21 позиция. Поиск 17 оставляет нужную карточку, раскрытие показывает «Уравнения», коды и источник, сброс возвращает 21. `08c` фиксирует верх страницы; **личный визуальный вывод о позиции 17 основан на отдельном полном close-up `08d-basic-exam-position17-detail.png`**. В нём читается формулировка спецификации и ссылка «Печатная с. 11 · лист PDF 6».
- Обычное поле ответа при 1280×720 также полностью видно: низ 684.69 px, шрифт 14 px. Поиск имеет высоту 39 px, шрифт 13 px, значок выровнен по вертикальному центру; горизонтального переполнения страницы нет.
- Настоящий повторный запуск EXE сохраняет run/report, связанные занятия и документ. Непроверенные ответы не возникают после восстановления.

Лично просмотрены все **14 PNG** из `report.json`, включая паузу, возобновление, перерыв, итог, фактический HTML, историю, карточку 17, поиск и восстановленную библиотеку документов. Материально нечитаемых значений или наложений, мешающих проверенному маршруту, не обнаружено. Поле дневного ответа теперь видно целиком. В написанном HTML исправлена подпись: отчёт составлен из сохранённых действий, а мнение OpenAI упомянуто условно, только если такое содержимое есть. Эти исправления и отсутствие ложных отметок этапов подтверждены новой серией; прежняя SHA `08cd147c…` с замечаниями сохранена в `studio-day-native-0.5-final`.

Оставшиеся границы и наблюдения:

- В коротком окне верхняя часть дневного плана занимает заметную высоту, лента блоков прокручивается горизонтально внутри своей области, содержимое комнаты — вертикально внутри комнаты. Это не размещение всего урока одновременно без прокрутки. На некоторых снимках видна только нижняя часть вступительного сообщения, поскольку внутренняя переписка прокручена к форме ответа.
- После экспорта временное уведомление перекрывает небольшую область нижнего содержимого на нескольких кадрах; сохранённый документ и контролируемые поля при этом проверены отдельно.
- Пять секунд реальной активности округляются в интерфейсе до 0 минут; исходное значение 5000 ms в отчёте сохранено.
- Проверены программный режим, локальная работа плана и реальный HTML. Не проверялись в этой серии живой преподаватель, содержательное освоение темы, все анимации, другие форматы документов, NSIS-установка, Windows DPI или частота кадров. Подпись «Точка −4» не попала в unsigned-сценарий и здесь не засчитана как визуально проверенная.

Все созданные окна, включая окно экспортированного HTML, закрыты; очередь native передана root до завершения этого документа.
