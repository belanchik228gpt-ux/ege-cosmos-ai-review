# Экзаменационная серия — интеграция с управляемым тестовым backend

9 сентября 2026 года, около 00:35 МСК. **Это проверка логики с фиксированными ответами, не результат живого OpenAI.** Настоящий App открыт в headless Chrome с отдельным пустым профилем. `window.cosmos` заменён контролируемым тестовым транспортом, включая явно тестовый статус подключения. Состояние обучения создаётся только действиями интерфейса; загрузка/сохранение идут через тестовый адаптер localStorage. EXE, аккаунт пользователя, системный курсор и настоящие модели не использовались.

**Итог: 6 групп PASS, 0 ошибок страницы.** Точная версия `CloudRoom.tsx` закреплена SHA-256 в `report.json`: `cd1f38018ee641f2df38d5e418792d75add4204ecc9afd398dd76f2673a94820`.

Проверенный сценарий:

1. Через «Как решать ЕГЭ» создана серия № 3 базовой математики на три примера. `topicId=exam-math-3`; контекст урока модуля не подмешан. Начальная выдача примера не увеличивает счётчик разбора.
2. Тестовый ответ `phase=review` на текстовую попытку без изображения не засчитывает математический пример как разобранный; «Следующий пример» остаётся недоступным.
3. На настоящем canvas листа проведён штрих через браузерные pointer-события и введено пояснение первого примера. Кнопка проверки создаёт реальный PNG (48 221 байт в первом листе). После ответа `phase=review` с фактическим вложением `completed=[1]`. Повторное открытие листа сохраняет его пояснение и штрих.
4. После завершённого разбора дополнительно прикреплено фото старого примера, затем выполнен переход к примеру 2. Новая выдача не получает ни старое изображение, ни его подпись; последующая подсказка также не получает это изображение. Номер увеличивается ровно один раз, `completed` остаётся `[1]`.
5. Лист второго примера открывается пустым. В него добавлены другой штрих и другое пояснение. В localStorage существуют отдельные ключи `:exam-1` и `:exam-2`; первый черновик не изменён. PNG второго рисунка отличается от первого.
6. Контролируемый отказ текстового запроса сохраняет номер 2 и `completed=[1]`. «Повторить запрос» отправляет тот же контекст без дублирования пользовательской реплики и не добавляет разбор.
7. Долгий запрос остановлен настоящей кнопкой отмены. После отмены тестовый backend намеренно возвращает поздний `phase=review`; этот ответ не попадает в сообщения или прогресс. Повтор отменённого запроса сохраняет номер и не дублирует реплику.
8. Контролируемый отказ проверки второго листа сохраняет PNG и номер. Повтор передаёт точно то же изображение; успешный `phase=review` добавляет только текущий пример: `current=2`, `completed=[1,2]`. Независимые учебные попытки не созданы, объект освоения тем не изменился.

В журнале сохранены 11 обращений к **фикстуре**: последний текст, наличие вложения, имя/длина изображения и отсутствие строки контекста модуля. Зафиксирована одна отмена. Это не количество живых модельных ответов.

Лично просмотрены все четыре итоговых PNG: первый разобранный лист, изолированный второй пример, состояние после отмены/повтора, второй разобранный лист. Счётчик визуально меняется с «Разобрано 1» на «Разобрано 2» только в нужных точках. Каждый ответ содержит видимую подпись «Тестовый фиксированный ответ». Кадры fullPage получены из viewport 1280×900; они не доказывают размещение всей переписки в одном экране. Внутренняя прокрутка переписки сохраняется.

Найденный и исправленный дефект: переход к следующему примеру правильно исключал старый PNG из запроса, но сохранял у переходной реплики старое `imageName`. Исправлена одна строка `CloudRoom.tsx`: при `nextLesson` название неприсланного старого фото больше не записывается. Исходный FAIL и снимок сохранены отдельно в `../exam-series-browser-0.5.1-attempt-1/`; проверка не ослаблена. Последующие изменения QA сделали второй PNG отличимым от первого и стабилизировали положение закреплённой панели при fullPage-захвате.

Границы: не проверены живое распознавание рукописи, корректность ответа модели, итог всей серии, все предметы в режиме генерации, native EXE, установка или производительность. Слово «разобрано» здесь подтверждает обработку ответа `review`, а не правильность решения или освоение. Реальную модельную/native проверку выполняет root отдельно.

Скрипт: `scripts/exam-series-browser-review.mjs`. Собственные Chrome и Vite 5187 закрыты после каждого прогона.
