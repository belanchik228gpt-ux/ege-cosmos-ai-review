# Построение до самостоятельного ответа

8 сентября 2026, серия `2026-09-08T16:46:32.733Z`. Реальный React-компонент TeachingScene в headless Chrome; это не EXE и не вызов модели.

9/9 сценариев PASS. Во всех числовых сценариях `revealAnswer=false`, но build запускает построение: путь, движение маркеров, схождение в нуле, шкала неотрицательной длины. Финальный числовой ответ, количество различных корней и вычисленные подписи точек скрыты. Ось с числовой шкалой доступна для самостоятельного рассуждения. Цепочка выражений отдельно помечена `workedExample:true` и показывает другой объясняющий пример с условием `x < a`.

Все девять пауз: точное совпадение decoded пикселей Δ0, стабильные геометрия и время CSS/WAAPI. Проверены назад/вперёд/повтор, low/static, ширина390 (`scrollWidth=390`). Наличие истинного движения проверено отдельно от текстовой смены стадии: у числовых сцен есть активный `ts-traveller[data-move=true]` на build. Нулевая длина является вырожденным случаем без изменения координаты.

Лично просмотрены девять `*-final.png`, `zero-roots-paused-a.png`, `expression-narrow.png`, `zero-independent.png`. Результаты на рисунках соответствуют переданным значениям. Широкая панель от800px показывает рисунок и пояснение в двух колонках. Перекрытий и горизонтального обрезания в просмотренной серии нет; на390 мелкие координатные подписи уменьшаются. В conclude без внешнего narration возможен повтор вопроса двумя строками — замечание передано root.

Содержательный валидатор expression проверяет безопасную форму данных, но не доказывает произвольные преобразования. Интегрированное переключение teaching step, оценка самостоятельности и нативный EXE проверяются отдельно. Собственные Chrome/Vite5177 закрыты после прогона.

На момент проверки TeachingScene SHA256 `298a35e8b3d1ad835bf7914a6f3590fc2e394762d5c0815ee64155f1ef6b853f` (форматирование после прогона без изменения поведения); CSS `3545c3cef453d824919b76fcc65a55c3590a5135ebf8e247e39418627da84b5c`.
