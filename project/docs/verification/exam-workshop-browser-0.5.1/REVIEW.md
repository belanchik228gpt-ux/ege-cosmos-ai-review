# «Как решать ЕГЭ» — браузерная проверка 0.5.1

Проверено 9 сентября 2026 года, 00:20 МСК (`report.json` хранит UTC 8 сентября 21:20). Текущий настоящий App запущен через Vite в отдельном headless Chrome. Приложение EXE и модель не запускались, системный курсор не использовался, профиль браузера изолирован. Все изменения состояния сделаны через пользовательский интерфейс; учебные данные и ответы не внедрялись.

**TypeScript — PASS. Браузерный сценарий — 4 группы PASS, 0 pageErrors.**

Проверено:

- Навигация из бокового меню в новый раздел. Математика базового уровня: 21 номер; русский язык: 27; история: 21; обществознание: 25. Всего 94 карточки берутся из `src/domain/exam-workshop.ts`, без демонстрационных оценок или историй.
- Выбор математического № 3, русского № 27, исторического № 21 и обществоведческого № 25. Открываются требования, формат ответа и авторские советы; источники имеют отдельную ссылку, дату и физические страницы PDF спецификации/демоверсии. Вкладки поддерживают стрелки клавиатуры; выбранный номер каждого предмета сохраняется при переключении между предметами.
- Числа 0 и 31 отключают старт; допустимая серия содержит 1–30 примеров, начальное значение 10. Быстрый выбор 10 работает.
- Кнопка открывает настоящий CloudLesson математического задания № 3 с `mode='own'`, `total=10`, `current=1`, `completed=[]`, `messages=[]`. После настоящей перезагрузки страницы сохранённая серия восстановлена гидратацией без потерянных параметров или вымышленных завершений. В комнате видны «Пример 1 из 10», ноль разобранных примеров и кнопка «Начать серию».

Лично просмотрены все семь PNG из отчёта:

1. `01-math3-1280.png`: предметы, сетка номеров, требования и карточка серии.
2. `02-russian27-format.png`: формат сочинения, максимум баллов и ссылки на документы.
3. `03-history21-tips.png`: авторские советы по аргументации отделены от официальных требований.
4. `04-social25-requirements.png`: длинные требования и условия оценки переносятся без обрезки.
5. `05-math3-narrow.png`: полная страница при ширине 390 px; сетка номеров, источник и параметры серии адаптированы к одной колонке.
6. `06-series-controls-narrow.png`: реальный viewport 390×844 у настройки количества и кнопки начала; элементы читаются и помещаются по ширине.
7. `07-training-room-narrow.png`: существующая учебная комната с правильными номером, длиной серии и нулевым прогрессом.

Горизонтального переполнения страницы нет. Не обнаружены материально нечитаемые подписи или перекрывающиеся элементы нового раздела. Полная страница длиннее одного экрана: выбор номера, чтение требований и запуск тренировки доступны последовательной вертикальной прокруткой. Большинство PNG — fullPage, полученные в viewport 1280×720 либо 390×844; это не утверждение, что весь экран помещается в высоту 720 px. Положение закреплённой верхней панели на fullPage-кадрах зависит от текущей прокрутки; новый раздел не перекрывается ею в проверенных точках взаимодействия.

Границы: проверка создания серии и восстановления не подтверждает качество живой генерации примеров, оценку письменного ответа или прохождение всей серии. Все 94 карточки подключены, но лично визуально прочитаны четыре выбранных номера; проверку содержания PDF и доменные тесты ведёт отдельный модуль данных. Здесь не проверены packaged EXE, NSIS, GPU fallback, DPI, частота кадров и внешний переход по каждой ссылке.

После выполнения headless Chrome и собственный Vite 5186 закрыты. Скрипт: `scripts/exam-workshop-browser-review.mjs`.
