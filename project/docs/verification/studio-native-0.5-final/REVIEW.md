# Studio 0.5 — общий native smoke, итерация SHA 08cd

8 сентября 2026. Настоящий `release/win-unpacked/EGE Cosmos.exe`; SHA-256 `app.asar`: `08cd147c9983500c7eebb1aff4f8fafc1bd72bef393f49af9c7cced9aa20a859`. Изолированный профиль `studio-native-1788895762769`. Название папки обозначает эту итерацию проверки, а не обещание, что последующие изменения продукта уже проверены.

`scripts/studio-native-review.mjs`: **7/7 групп PASS**, exit 0, `pageErrors: []`. Проверены запуск без модели, настоящий статус runtime без входа, отсутствие оценки неотправленного черновика, точное открытие темы 1.7, фильтрация каталога и восстановление пустой выдачи, меню и отсутствие горизонтального переполнения на 700 px, изоляция четырёх предметов и полное сохранение `cloudSessions` после закрытия/повторного запуска EXE.

В математическом каталоге сценарий явно переключается из путеводителя базового ЕГЭ в «Общий каталог тем». Это адаптация к новой навигации, а не замена проверки открываемой позиции: идентификатор `school-math-1-7` по-прежнему проверен в сохранённом состоянии.

## Личный просмотр

Все четырнадцать PNG `01`–`14` просмотрены. Главная, четыре комнаты, подключение, подробная карточка темы с источниками, каталог, настройки, документы и план читаются. Длинные названия укладываются в свои области; горизонтальной обрезки основной информации не видно. История комнаты использует собственные предметные темы. Новая подпись модуля честно отделяет авторский первый шаг от полного официального названия позиции 1.7.

Два замечания предыдущей сборки исправлены в видимых кадрах этой итерации:

- `08-catalog-grade11.png`: значок поиска и ввод находятся в одной строке; значок расположен слева.
- `12-room-1280.png`: обычная математическая комната показывает поле ввода в первом viewport 1280×720. Дополнительное измерение в соседнем day-прогоне: textarea 744×62 px, y 622.6875–684.6875, шрифт 14 px.

`13-room-narrow.png` подтверждает читаемость верхней части на 700×900 и отсутствие горизонтального переполнения. Длинная комната на таком экране требует вертикальной прокрутки; одновременная видимость всей комнаты и ввода не утверждается.

## Границы

Вход и генерация модели не выполнялись. Проверка не подтверждает качество живого диалога, вывод математических формул моделью, работу OCR, PDF/DOCX или установленного NSIS. Подробные URL источников в этой серии не открывались. Для учебного дня и реального HTML существует отдельный отчёт `../studio-day-native-0.5-final/REVIEW.md`.

После этой итерации root сообщил о трёх узких изменениях: макет учебного дня на коротком экране, условная подпись HTML-отчёта и уточнение подписи точки числовой прямой. Они не включены в SHA 08cd и требуют новой проверки под новым хешем. История прежнего кандидата и его неудачных QA-попыток сохранена в других папках.

Все окна этого сценария закрыты; последующий просмотр выполнялся только по сохранённым PNG.
