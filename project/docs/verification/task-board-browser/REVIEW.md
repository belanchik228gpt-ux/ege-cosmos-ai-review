# Доска фактических частей задания

8 сентября 2026. Последний прогон `2026-09-08T16:47:58.338Z`, [results.json](results.json). Это настоящий TaskBoard и стили приложения в headless Chrome, без модели и нативного EXE.

Вместо универсальных «1 / 2 / ?» доска извлекает буквальные части `task.prompt`. Русский: слова предложения и уже имеющиеся связующие слова; отсутствующая запятая не добавляется. История: названное событие, роль, дата, место, без добавления неназванного правителя или даты. Обществознание: реальные участники, изменение и величины условия. Математика: фактические выражения, числовые данные и названные связи, например `BC = 14 см` и `AM — медиана` одновременно. Ответ задания не передаётся извлечению содержания. Это ограниченная визуальная разбивка условия, не автоматический предметный анализ любого текста.

Проверены 13/13 модульных тестов и полный tsc. После просмотра исправлены два случая потери данных: изменение цены больше не заменяется вопросом о спросе, а у BC=14 сохранена явная связь AM — медиана.

Браузерный прогон: **8/8 PASS** — два русских, два исторических, два обществоведческих и два математических задания. В каждом autoplay перешёл к подсветке фактических частей; на паузе оставались от4 до16 активных остановленных переходов. Сравнение всей `.sb-body`, включая псевдоэлемент подчеркивания, дало **decoded Δ0** во всех восьми парах. Геометрия и animation.currentTime неизменны. Проверены следующий/предыдущий шаг и повтор, отсутствие скрытого ответа в тексте, отсутствие вставленной запятой и имени князя.

Проверены выбор0,5× и2×, low/static, reduced-motion и внешняя пауза занятия. В static движения нет; ручные кадры доступны. Внешняя пауза блокирует управление. На390px `scrollWidth=390`, карточки и кнопки переносятся, горизонтального переполнения нет. Высокая узкая доска требует вертикальной прокрутки; это ожидаемо.

Лично просмотрены все восемь финальных кадров первой серии, market-narrow/static и обновлённый `math-median-final.png` повторной серии. Текст и числа читаются, важные величины подсвечены, блоки не перекрываются. Отдельной предметной иллюстрации произвольной неизвестной темы этот fallback не обещает: используются факты условия и осмысленные иконки.

Источники на момент финальной серии: TaskBoard `8a9a394d394214fdbe40cf4919a55a103490c56f5cc2e2f62a6f6d1686cece5f`, task-board-content `4b01b48d0057a72de74f342b58b3923d4ffc810c70d3ae5ff6901e05269fb255`, CSS `2fa036cf1c4797bf3d11399a0b11ff1f3d7f89b96ed1ec0f216bdc82ef0c7ca7`.

Не проверено этим стендом: связка TutorRoom/controller, нативная упаковка и действительная точность ответа модели. Эти проверки ведёт root отдельно. Собственные Chrome и Vite5177 закрыты по завершении.
