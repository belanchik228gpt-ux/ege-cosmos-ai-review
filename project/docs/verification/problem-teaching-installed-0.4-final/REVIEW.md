# Финальный повтор «Своей задачи» на упакованном CSS

2026-09-08 выполнен настоящий `C:\new cosmos\test-results\installed-app\EGE Cosmos.exe` версии 0.4.0. В отличие от проверки кандидата, **CSS не внедрялся и состояние не подменялось**: использованы файлы нового приложения, чистый профиль и реальные элементы интерфейса.

- ASAR SHA-256: `375ff6d3937fbd7e4a0232692433c94d45cf3571cac4e8467aa0492489b982c3`.
- EXE SHA-256: `6bcfd6828f3f672ed78db9f4fd5792b0c518ccdcb88cfb57c3513b6e31164e8e`.
- Результат: **9/9 сценариев PASS**, `layoutFailures: []`, ошибок страницы нет, скрипт завершился с кодом 0.
- Все три собственных процесса закрыты. Пользовательский профиль не менялся, окна параллельных проверок не закрывались.

Файлы EXE/ASAR обновлены в существующей установочной папке. Сам запуск NSIS этой проверкой не выполнялся; пригодность установщика и равенство его файлов подтверждаются отдельной проверкой доставки. Наличие слова `installed` в имени каталога само по себе не является доказательством чистой установки.

## Поведение

1. Приложение работает без доступных тестовому профилю моделей. Пустой runtime вызывает конечный честный отказ модельного объяснения; учебный шаг сохраняется и доступен.
2. Подсказка для прямоугольника 7×4 открывает вопрос о числе клеток в одном ряду с фактическим числом 7 на сцене.
3. Неверное 4 отклоняется, `7?` принимается для текущего шага. Это не завершает задачу и не увеличивает счётчик итоговых попыток.
4. После настоящего закрытия/запуска процессa принятый шаг восстановлен через «Мои примеры»; «Далее» продолжает с выбора действия.
5. «Умножение» → `28?` → «Далее» завершают пример без повторного ввода итогового ответа. Одна итоговая попытка, результат отмечен с помощью.
6. В `|x − 2| = 3` пройдены центр, знак расстояния и направление; принимаются все корни `5; -1?`.
7. В `|x − 2| = -3` принимается «нет решений», невозможное отрицательное расстояние не превращается в корни.
8. Файл фотографии выбран настоящим input. Недоступный OCR честно сообщил об отказе; условие вручную введено и подтверждено. После второго настоящего перезапуска восстановлены признак фото, подтверждённый текст и принятый промежуточный шаг. Сырое изображение не записано в учебную память.
9. Проверены узкая раскладка и изоляция: четыре собственных примера, три завершены с помощью; предметных сессий и модельных сообщений нет.

## Проверка геометрии и личный просмотр

Все **13 PNG** открыты и просмотрены лично: 11 полностраничных и два снимка видимой области 1280×720. Поле ответа и отправка больше не обрезаются границей карточки. На девяти широких состояниях, где есть диалог, его нижняя граница равна 844.39 px, нижняя граница отправки — 825.39 px; остаётся 19 px запаса. В узкой раскладке запас 23 px. Автоматическая проверка контролировала верхнюю и нижнюю границы textarea и кнопки относительно панели; ни одного выхода нет.

Это не утверждение, что весь экран помещается без прокрутки. В исходном положении страницы при 720 px поле ввода находится ниже края viewport, что прямо видно на двух снимках видимой области; страница прокручивается. История сообщений имеет отдельную внутреннюю прокрутку. При 620×844 диалог размещён ниже сцены. Эти ограничения остаются, но прежней обрезки composer самой панелью больше нет. Текущие числа, модуль, центр и вопросы читаются; горизонтального выхода основного содержимого не обнаружено.

Промежуточная геометрия по-прежнему представлена крупной анимированной записью, а полный прямоугольник — на основной сцене. Общий заголовок кадра «Определи центр» у сцен направления/знака можно точнее связать с вопросом в следующем изменении; проверяемый вопрос и параметры актуальны.

## Границы и история исправления

Прогон не принимает качество живой Qwen, OCR, произвольные неподдерживаемые задачи, весь курс или установщик. Для фото проверена ручная расшифровка после реального отказа отсутствующего runtime. Все решения с подсказками честно помечены выполненными с помощью.

Предыдущая сборка `3eeb…` дала 9/9 по поведению, но получила визуальный FAIL: `../problem-teaching-installed-0.4/REVIEW.md`. Этот результат сохранён. Кандидат 420 px отдельно проверен через временный CSS: `../problem-composer-candidate-0.4/REVIEW.md`. Настоящий повтор с `375ff6…` подтверждает исправление уже в упакованном приложении. Продукт во время финальной проверки не менялся.
