# Последняя визуальная проверка установленного EXE

**PASS в указанном объёме.** Проверка 9 сентября 2026 года, окончательный ASAR `c6b16a9d970c941e507f04eb57ca05b49476ca7b1b403235da498d708e21ce0c`. Это реальное установленное приложение, а не браузерная имитация. Все шесть снимков из `report.json` лично просмотрены.

- Короткая формула теперь крупная и по центру: при 1920×1080 фактический размер KaTeX — 60 px. Она не обрезана и не прижата к нижнему краю. Сохраняются исходные данные настоящего ответа OpenAI. Пауза удерживает кадр, «Следующий»/«Предыдущий» меняют кадр, Esc возвращает в тот же разговор.
- Рукописный лист после переустановки восстанавливает все три строки `2x + 3 = 11`, `2x = 8`, `x = 4`. В профиле сохранены 30 штрихов двух проверявшихся черновиков.
- В «Как решать ЕГЭ» проверены четыре предмета: математика — 21 номер, русский — 27, история — 21, обществознание — 25. Лично просмотрены карточки математики № 3 («Оформление»), русского № 27 («Требования»), истории № 21 («Советы и ошибки»), общества № 25 («Требования»). Названия, активный номер, цвет, содержание и источники соответствуют предмету.
- Ошибок страницы не было. Дополнительных запросов модели этот целевой прогон не делал: использован ранее фактически полученный ответ, сохранённый обычным приложением. Исходное живое прохождение описано отдельно в `../math-sheet-installed-0.5.1/REVIEW.md`.

Скрипт `scripts/fullscreen-installed-review.mjs` создаёт снимки настоящего EXE и проверяет поведение управления. Профиль обучения изолирован. Внешние нативные клики отключены только у QA-окна из-за повышенного AutoClicker; системный процесс не менялся. Полный просмотр всех 94 текстовых карточек в этом native-прогоне не проводился: проверены размеры списков и четыре конкретные позиции; остальные источники и карточки имеют отдельную доменную и браузерную проверку.

В кандидатном прогоне скрипт первоначально ошибочно требовал больше 30 штрихов, хотя фактически сохранено ровно 30. Это дефект предположения QA-скрипта, не потеря черновика. Исходный результат оставлен в `../fullscreen-candidate-0.5.1/harness-count-assumption.json`; после исправления условия кандидатный и установленный прогоны прошли.
