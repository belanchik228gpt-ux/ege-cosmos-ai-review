# Studio 0.5 — итоговая проверка основных экранов без GPU

Дата: 8 сентября 2026 года. Проверен настоящий упакованный `release/win-unpacked/EGE Cosmos.exe`, ASAR SHA-256 `d75fb7d4e00fae1ec67e8c4bb5261ed6da91f5f3b481b890ecfbcf744ca67cfe`. Это проверка unpacked EXE; прохождение установки NSIS не входит в эту серию.

**Результат: 7/7 проверок PASS, 0 pageErrors.** Подробные assertions и пути находятся в `report.json`. Приложение запускалось с `--disable-gpu`; Electron подтвердил `gpu_compositing: disabled_software`, `rasterization: disabled_software`, `webgl/webgpu: unavailable_software`. Таким образом, проверен фактический программный режим, а не только наличие флага запуска.

Все действия выполнены через интерфейс в отдельном новом профиле. Учебное состояние и модельные ответы не подставлялись. Проверено реальное состояние «вход не выполнен», открытие окна подключения и его закрытие; вход и генерация ответа модели намеренно не выполнялись.

Проверено:

- Открытие приложения и четырёх предметных комнат без ожидания модели; честное обозначение доступных материалов и отсутствующего входа.
- Сохранение неотправленного текста как черновика без начисления ошибочной попытки.
- Поиск точного пункта 1.7 и переход к авторскому начальному уроку модуля; общий пункт действительных чисел не переименован в «Модуль».
- Фильтр класса, пустой результат и восстановление каталога; отдельный ориентир на 21 позицию базового ЕГЭ доступен рядом с общим каталогом.
- Узкое окно 700 px, мобильное меню и отсутствие горизонтального переполнения страницы.
- Изоляция четырёх предметов, сохранение занятий при настоящем завершении и повторном запуске EXE.
- Через «Настройки» выбраны низкое качество и уменьшение движения; оба значения подтверждены после перезапуска. В этой серии настройки изменены перед кадром 09, ранние кадры 01–08 сняты с начальными настройками, но также без GPU.

Лично просмотрены все **14 PNG** из `report.json`: 01–14. Главная, четыре комнаты, окно входа, детальная карточка темы, фильтр 11 класса, настройки, документы, план, урок 1280×720, узкий урок и главный экран после восстановления читаемы. Цветовые акценты предметов различимы, основная и вторичная навигация не перекрываются. В кадре 12 поле ответа находится в видимой области короткого окна; в кадре 13 текст адаптирован к узкому окну без обрезки справа. На высоких стартовых кадрах комнат нижняя часть формы находится ниже видимого края страницы и доступна прокруткой; эти кадры не являются доказательством размещения всей комнаты на одном экране.

Границы: это ограниченная визуальная и функциональная серия без живого ответа преподавателя, без проверки каждой темы и каждого кадра анимации. Она не доказывает 60 FPS, производительность слабого ноутбука, физическое отсутствие видеокарты, Windows DPI 200% или работу всех экспортных форматов. Незначительные цветные края некоторых глифов видны при программной растеризации; текст остаётся читаемым. Предыдущая серия SHA `08cd147c…` сохранена отдельно в `studio-native-0.5-final` и не переименована в результат новой сборки.

Окна и процессы этой серии закрыты; native-очередь освобождена до записи отчёта.
