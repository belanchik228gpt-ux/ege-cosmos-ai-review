# Учебный маршрут в установленном EXE 0.2.0

Фактический заключительный прогон 8 сентября 2026 года, 15:55:40–15:56:14 по Москве. `scripts/study-flow-review.mjs` запущен с `COSMOS_EXE` и ожидаемым `COSMOS_EXPECTED_ASAR_SHA`. Источник: установленное приложение `test-results/installed-app/EGE Cosmos.exe`, а не Vite или отдельный браузерный макет.

App bridge вернул `packaged: true`, `version: 0.2.0`, `platform: win32`. Перед запуском прочитан настоящий `resources/app.asar`: 30 841 635 байт; SHA-256 `aacc72fee1736e3de872d274dc183a59218fe28d9bfc07dd2a5e63fe5db17366`, совпавший с ожидаемым артефактом.

Все 9 функциональных сценариев прошли, ошибок страницы нет. Подробности и фактические данные находятся в `result.json`:

1. Первый вход, 10 класс, базовая математика, 120 минут с перерывами и четырьмя предметами.
2. Школьная тема про модули и старт связанного занятия одной кнопкой.
3. Четыре реальные проверенные попытки: одна ошибка, два верных ответа с помощью, один самостоятельно.
4. Переходы в настройки, материалы и календарь с восстановлением той же сессии и шага сцены.
5. Нативная активность, пауза и возобновление; отключение ввода, подсказки и отправки на паузе; переход через перерыв.
6. Досрочный итог с настоящей ошибкой и незавершёнными блоками, настоящий экспорт PDF кнопкой интерфейса.
7. Неделя/месяц, личная тема, изменение минут и перенос даты без скрытого роста бюджета.
8. Три новых диагностических ответа и переход из итога к своему плану.
9. Настоящее закрытие и повторное открытие установленного EXE с восстановлением памяти.

Для активности использовано настоящее окно Electron: bridge подтвердил `visible: true, focused: true`. Счётчик блока вырос с 0 до 5000 мс; после постановки на паузу не изменился за 5600 мс; после продолжения вырос до 10000 мс. В итог вошло 10000 мс — учёт блока и связанной сессии не суммирован дважды. Карточка итоговой сборки показывает «10 с». На паузе поле ответа, подсказка и отправка действительно отключены, число попыток не изменяется; после продолжения поле и подсказка снова доступны. Это короткая проверка; длительная работа, простой свыше 90 секунд и уход фокуса требуют отдельных сценариев.

PDF создан именно кнопкой «Сохранить отчёт в PDF», затем прочитан с диска. Размер 53 082 байта; сигнатуры `%PDF-` и `%%EOF` присутствуют; SHA-256 `aea6048835a9a450426a81ab2f812f3edc15b49ec1502e9cd69bffa9d1e31c5a`. Копия фактического файла — `early-study-report.pdf`; исходный путь и запись документа находятся в `result.reportPdf`. После перезапуска восстановлена и запись этого документа. Файл передан learning_core для отдельного рендера и визуальной проверки страниц; проверка сигнатуры сама по себе не объявляется визуальной приёмкой PDF.

Модель не запускалась: runtime переопределён на отсутствующий каталог внутри изолированного тестового профиля. Локальные ответы заданий проверены как локальные; live LLM этой проверкой не подтверждается. Учебное состояние создавалось исключительно через интерфейс.

Все восемь PNG получены из реального renderer установленного EXE. Имена `*-1280.png` обозначают внешний размер окна 1280×720; область содержимого Windows имеет viewport 1264×681. Имена `*-620.png` обозначают минимальный внешний размер 620×844; viewport604×805. `fullPage` сохраняет прокручиваемое содержимое целиком. Полные размеры записаны в JSON; это не снимки полного рабочего стола Windows.

Лично просмотрены итоговые PNG досрочного отчёта и календаря на минимальной ширине; урок просмотрен также в предшествующем нативном проходе. Формулы, задания и статусы читаемы. Горизонтального переполнения документа автоматическая проверка не обнаружила. Найденный ранее край раскладки кнопок календаря исправлен root и перепроверен в заключительной сборке: при `innerWidth=604` все 21 кнопка семи блоков находится внутри своего редактора. Правая граница редактора 542 px, правая граница крайней кнопки также 542 px, ширина каждой кнопки 35 px. На новом `08-plan-620.png` кнопки занимают отдельную строку и не обрезаются. Геометрия сохранена в `result.narrowControls`; это дополняет личный просмотр реального PNG.

После прогона оба экземпляра, открывавшиеся для проверки перезапуска, закрыты штатным завершением приложения. Нативная очередь передана root для его отдельного сценария; новых окон этот скрипт после завершения не открывал.
