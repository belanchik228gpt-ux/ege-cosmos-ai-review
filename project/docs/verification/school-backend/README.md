# Backend-проверка школьного режима

Дата: 2026-09-09. Это проверка исходников и реальных локальных ресурсов, **не native-приёмка нового EXE и не оценка живой модели**.

Манифест: `resources/school-program/manifest.json`.

SHA-256: `fc678dc9d35a9a8e727cbc73fbed82424f548804108f82d30ad877721ee64d28`.

`resource-check.json` получен командой `node scripts/prepare-school-sources.mjs --check`. Проверены 27 исходных PDF, их размеры и SHA-256, целостность соответствующего TXT через runtime-сервис, допустимость каждой записи и чтение отрывка. Всего 3478 физических страниц, 35 048 810 байт PDF, 11 343 560 байт TXT. Источники представлены для 15 предметов: 25 программ, методическое письмо общества и нормативная опора индивидуального проекта.

Отдельный запрос настоящему файловому сервису:

```json
{"id":"fgos-project-requirements","page":27,"maxChars":4500}
```

Результат: `ok: true`, `kind: standard`, `status: official-standard`, первая физическая страница 27, общий размер отрывков 4500 символов, `truncated: true`. Текст первой страницы содержит «Индивидуальный проект». Проверка не заменяет правовую экспертизу актуальности всего нормативного акта и не выдаёт этот пункт за полную школьную программу проекта.

Целевые тесты:

```powershell
node node_modules/vitest/vitest.mjs run tests/school-desktop.test.ts tests/school-sources.test.ts tests/openai-service.test.ts tests/references.test.ts
```

Последний результат: **80/80 PASS**, четыре файла. Экспортные тесты действительно записывают TXT/MD/HTML/DOCX во временную изолированную папку и читают созданные файлы. Проверки OpenAI transport используют тестовый child process; PDF-просмотрщик — тестовый объект окна. Эти два вида тестов не означают успешный вход, живую генерацию или визуальную проверку Electron.

Проверены отсутствие подмены предмета математикой, допустимые классы, сохранение прежних инструкций ЕГЭ, независимая школьная история, отдельные школьные папки экспорта, подмена текстового и PDF-файла того же размера, traversal/junction, типы источников, неизвестные ID, пустая библиотека и ограничения отрывков. Сборку и окончательную проверку просмотра PDF из установленного приложения проводит основной агент отдельно.
