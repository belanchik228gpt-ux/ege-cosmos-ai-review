# Контрольная точка школьного режима 0.6.0

09.09.2026. По просьбе пользователя проверяется остаток недельного лимита; при 1% работа прекращается после сохранения состояния. Автоматическое использование сброса не разрешено.

Реализованы отдельный режим «Школа», каталог 337 разделов / 15 предметов / 8–11 классы, 27 локальных официальных PDF, школьные занятия OpenAI, план, память и документы. ЕГЭ сохранён отдельно. Полнота многозанятийных курсов и безошибочность будущих ответов не заявляются. Тексты учебников не скачивались; сохранены библиографические ссылки.

Проверки: 784 автоматических теста в 55 файлах; production build; реальные ответы модели по физике, химии и английскому; EXE-снимки, PDF, перезапуск и изоляция ЕГЭ. Ранняя функциональная проверка нашла визуальные дефекты; они исправлены, последующие результаты сохраняются отдельно.

Установка завершена, установленный EXE проверен повторно. Упакованная версия находится в `release/win-unpacked`; установленный путь — `test-results/installed-app/EGE Cosmos.exe`. Desktop-ярлык указывает на этот EXE. Финальный app.asar SHA-256: `6145adcdd91a82cf1543f5dcc40bf428e34ac3fd6d4213aff4bf2536c131835a`.

Воспроизводимые проверки: `scripts/school-native-review.mjs` (новый QA-профиль; live по `COSMOS_QA_LIVE=1`), `scripts/school-visual-recheck.mjs` (повтор сохранённых настоящих QA-ответов), `scripts/verify-release.mjs`. Источники: `scripts/fetch-school-sources.mjs`, затем `scripts/prepare-school-sources.mjs --check`.

QA-профиль: `test-results/school-native-1788926849357`. Пользовательский учебный профиль не используется для тестов. Его исходный SHA-256: `2045f5c35a97841066a315e0a7e56d43fe3bfd502e3cb4b826811e8760cb280d`. Вход переиспользуется штатным runtime, credentials не копируются.

Функциональные результаты и реальные снимки — в `docs/verification/school-native-0.6.0`, исправленные визуальные результаты — `school-visual-0.6.0`, установленный EXE — `school-installed-0.6.0`. Ранний live-прогон явно отмечен как функциональный с последующими визуальными исправлениями. Установка не меняла личный учебный профиль. Все агенты завершили работу.

Блокеров выпуска школьного режима не осталось. Дальнейшая работа: педагогическая проверка многозанятийных курсов по всем разделам, расширение специализированных сцен, полные доступные на законных основаниях учебные тексты, проверка рукописных школьных ответов в реальном диалоге и длительные тесты масштабирования/GPU. Это отдельные неподтверждённые возможности, не скрытые PASS.

Код отправлен в существующий приватный репозиторий `https://github.com/belanchik228gpt-ux/ege-cosmos-studio`. Основная реализация: коммит `9a66d45`; итоговые протоколы сохранены последующим коммитом. Работа над этим этапом завершена до порога 1%, сброс лимита не использовался.
